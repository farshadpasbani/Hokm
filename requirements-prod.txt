# Runtime dependencies for the Telegram Mini App service (server.py).
# torch installs the CPU build via the extra index in the Dockerfile.
flask>=2.0.0
gunicorn>=21.0.0
numpy>=1.19.2
pandas>=1.2.0
torch>=2.13.0
# Security floor for a transitive dependency — filelock comes via torch.
filelock>=3.20.3
